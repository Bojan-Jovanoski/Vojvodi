@model IEnumerable<Models.VojvodaBitkas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bitka.Lokacija)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vojvoda.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bitka.Lokacija)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vojvoda.Name)
        </td>
    </tr>
}

</table>
