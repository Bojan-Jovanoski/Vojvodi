@model IEnumerable<Models.SecretName>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vojvoda.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vojvoda.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SnameID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SnameID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SnameID })
        </td>
    </tr>
}

</table>
